<UserControl x:Class="Desktop.Views.MoneyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Desktop.ViewModels"
             xmlns:local="clr-namespace:Desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:MoneyViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Button Content="Vissza" Height="25" Width="75" Margin="0,0,600,300" Command="{Binding ChangeToMainWindowCommand }"/>
        <Label Content="Pénznem" HorizontalAlignment="Center" Margin="0,100,0,0"/>
        <TextBox Height="25" Width="200" Margin="0,0,0,150"/>
        <ListBox Height="100" Width="200" ItemsSource="{Binding Moneys}" SelectedItem="Money"/>
        <Button Height="25" Width="75" Margin="0,150,100,0" Content="Mentés" Command="{Binding DoSaveCommand}"/>
        <Button Height="25" Width="75" Margin="100,150,0,0" Content="Törlés" Command="{Binding DoDeleteCommand}"/>
    </Grid>
</UserControl>
