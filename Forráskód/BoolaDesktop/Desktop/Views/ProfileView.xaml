<UserControl x:Class="Desktop.Views.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Desktop.ViewModels"
             xmlns:local="clr-namespace:Desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:ProfileViewModel/>
    </UserControl.DataContext>
   
    <Grid>
        <Button Height="25" Width="60" Margin="0,0,700,400" Content="Vissza" Command="{Binding ChangeToMainWindowCommand}"/>
        <Label Content=" Profil Név" Margin="0,0,0,200" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBox Height="25" Width="200" Margin="0,0,0,150" Text="{Binding Profile.Name}"/>
        <ListBox Height="100" Width="200" ItemsSource="{Binding Lista2}" SelectedItem="{Binding Profile}"/>
        <Button Height="25" Width="60" Margin="0,150,100,0" Content="Mentés" Command="{Binding DoSaveCommand}"/>
        


    </Grid>
</UserControl>
